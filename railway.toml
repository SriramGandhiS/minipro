[build]
builder = "dockerfile"

[deploy]
startCommand = "gunicorn -w 1 -b 0.0.0.0:5000 --max-requests 1000 --max-requests-jitter 100 --timeout 60 app:app"
healthcheckPath = "/"
restartPolicyMaxRetries = 5
